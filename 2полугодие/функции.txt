from itertools import product - штука для создания перебора
from turtle import * - черапшка

print(a)            # вывод переменной
print('baza')          # вывод текста

СПИСОК:
sp = [1,2,3,6,6,6,1337]
sp.pop(0) - убрать нулевой элемент из списка
s = s.replace('O','A') - заменить O на A
sp = n.split('можно указать разделитель', максимальное количество разделений) - разбивает строку на подстроки 
sp.append(b) - добавить элемент к списку
sp.index(3) - индекс элемента в списке
//sp.extend(sp) - добавить к списку элементы другого списка
reversed(sp) - инвертирует порядок элеметов списка
sorted(sp) - сортирует элементы списка по алфавиту, по возрастанию и т.п.
data.sort(reverse=True) - сортирует список в инвертированном порядке
sp.index(6) - выводит индекс шестёрки в итерируемом объекте
sp.count('6') - считает количество шестёрок в итерируемом объекте
ls = [x for x in range(10)] - генератор списка
len(sp) - длинна элемента a
Sp.add('python') - добавление элемента в множеств
  
ФАЙЛЫ:
with open('название файла') as f: - открытие файла
s = f.readline() - считать одну строку файла
s = f.readlines() - считать все строки файла в список

ЦИКЛ:
for i in range(начало, конец, шаг): - перебор значений i в диапазоне N
while условие:  - повторение цикла до тех пор, пока выполняется условие
break - прерывание исполнения цикла
exit() - завершение программы
while True:         # вечный цикл с выходом по break
    break



if (лог.выражение) == x: - если истина, то выполняется
elif (лог.выражение):
else: - без условия

nums = product('01234567', repeat = 5)	- число состоит из цифр от 0 до 7, 5-И значное
a = bin(i)[2::1] - срез[начало:конец:шаг]

Строка:
строка.join(список) - работа со списками. Объединяет строку в список 

print(f'123{x}5') - форматированный вывод
all(список или выражние) - проверяет все элементы на истиность (обязательно иттерируемый объект)

x % 2 - остаток
x // 2 - целое
a=2                   # присвоить значение 2
a!=2                  # не равно

bin(123) - переводит число в бинарный код
str(456) - преобразование к строковому типу
int('123') - преобразование к целочисленному типу
list(123) - преобразование к списку
len(sp) - вычислить длину итерируемого объекта
input(пояснение) - ввод
max(sp) - нахождение максимального значения
min(sp) - нахождение минимального значения
sum(sp) - суммирует все элементы
abs(144) - модуль числа
sqrt(169) - квадратный корень из числа
round(111.23) - округление float числа

ЧЕРЕПАВЕЛ
left(90)     # поворот налево 90гр
forward(300) # вперед на 300 единиц
right(120)   # поворот направо 120гр
pu()         # поднять перо
goto(x*30,y*30)  # переходить по точкам
dot(5)           # нарисовать точку          
done()           # запустить рисование
